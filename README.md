# jfrog-repo
### Install jfrog artifactory 
```yum install wget -y
yum install wget -y
wget -V
wget -O artifactory-pro.rpm "https://releases.jfrog.io/artifactory/artifactory-pro-rpms/jfrog-artifactory-pro/jfrog-artifactory-pro-[RELEASE].rpm"
sudo yum install ./artifactory-pro.rpm -y
sudo systemctl status artifactory.service
sudo systemctl start artifactory.service
sudo systemctl status artifactory.service
```

### Install jenkins 
```
sudo usermod -aG sudo tomuser
groups
sudo usermod -aG root tomuser
yum update
java -version
sudo useradd -m -U -d /opt/tomcat -s /bin/false tomcat
rm -R /opt/tomcat/
y
ll
sudo useradd -m -U -d /opt/tomcat -s /bin/false tomcat
cd /tmp
wget https://mirror.kiu.ac.ug/apache/tomcat/tomcat-9/v9.0.45/bin/apache-tomcat-9.0.45.tar.gz
curl -O https://mirror.kiu.ac.ug/apache/tomcat/tomcat-9/v9.0.45/bin/apache-tomcat-9.0.45.tar.gz
sudo mkdir /opt/tomcat
tar -xf apache-tomcat-9.0.45.tar.gz
ll -hltr
tar cvf apache-tomcat-9.0.45.tar.gz 
tar xvf apache-tomcat-9.0.45.tar.gz 
tar -xvf apache-tomcat-9.0.45.tar.gz 
tar -xvzf apache-tomcat-9.0.45.tar.gz 
file apache-tomcat-9.0.45.tar.gz 
sudo tar -xvzf apache-tomcat-9.0.45.tar.gz 
sudo tar -xvzf apache-tomcat-9.0.45.tar.gz -C /opt/tomcat/ --strip components=1--
sudo tar -xvzf apache-tomcat-9.0.45.tar.gz -C /opt/tomcat/ --strip components=1
sudo tar -xvzf apache-tomcat-9.0.45.tar.gz -C /opt/tomcat/ --strip components=1
sudo tar -xzvf apache-tomcat-9.0.45.tar.gz -C /opt/tomcat/ --strip components=1
yum seacrh apache
yum search apache
yum search apache | grep tomcat
rm -R /opt/tomcat/
yum install tomcat
ll /etc/tomcat/
vi /etc/tomcat/tomcat.conf 
cd /etc/tomcat/
ll
grep -lri 8080
vi server.xml 
systemctl restart tomcat
systemctl status tomcat
iop a
ip a
ll
grep -lri 8080
grep -lri port
vi tomcat
vi tomcat.conf 
sudo firewall-cmd --zone=public --permanent --add-port=8888/tcp
sudo firewall-cmd --reload
ip a
vi server.xml 
systemctl restart tomcat
vi server.xml 
sudo firewall-cmd --zone=public --permanent --add-port=8443/tcp
sudo firewall-cmd --reload
systemctl restart tomcat
systemctl status jenkins
systemctl stop jenkins && systemctl stop wildfly && systemctl stop docker
vi server.xml 
systemctl restart tomcat
getenforce
setenforce 0
ip a
ll
grep -lri 8080
grep -lri 8090
grep -lri 80
vi web.xml 
ps -ef | grep tom
netstat | grep 8080
netstat -ntulp | grep 8080
sudo firewall-cmd --zone=public --permanent --add-port=8080/tcp
sudo firewall-cmd --reload
ip a
sudo systemctl enable tomcat
history > created-tomcat.sh
reboot
systemctl status tomcat
netstat -ntulp 
systemctl status docker
ip a
systemctl stop jenkins && systemctl stop wildfly && systemctl stop docker
systemctl restart tomcat
getenforce
setenforce 0
getenforce
systemctl status apache
netstat -ntulp 
systemctl stop tomcat
netstat -ntulp 
systemctl start wildfly
netstat -ntulp 
netstat -ntulp 
systemctl stop wildfly
netstat -ntulp 
systemctl start tomcat
netstat -ntulp 
getenforce
vi /etc/tomcat/server.xml 
sudo firewall-cmd --zone=public --permanent --add-port=8888/tcp
sudo firewall-cmd --reload
systemctl restart tomcat
```
